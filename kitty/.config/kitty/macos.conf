editor /opt/homebrew/bin/nvim
macos_option_as_alt left

# Remote-control socket for `kitty @` / global keybindings.
# Relative path: kitty resolves it against the (writable) temp directory.
listen_on unix:omarchy-kitty-{kitty_pid}
