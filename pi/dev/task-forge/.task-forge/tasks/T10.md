## Execution Report — T10

### Summary
Eliminated the V1 runtime authority import from `index.ts` and replaced every usage with V2-only logic or plain fallbacks. The two quarantined V1 helper files were moved into `src/migration/` so they are strictly isolated to migration-only usage.

### Files modified
- **`index.ts`** — Removed the `./src/v1-runtime-quarantine` import and all call sites:
  - `reconcileFromAuthoritative`: dropped `syncLegacyStateView`, replaced `legacyStatusLabel(state)` with `"forge:idle"`.
  - `syncExecutionSnapshot`: replaced `computeEffectiveStatus` / `buildExecutionFacts` with `authoritative?.status ?? "idle"`.
  - `persistState`: replaced `legacyStatusLabel(state)` with `"forge:idle"`.
  - `phaseIntegrationReview`: replaced `buildLegacyStateView(authoritative).tasks` with an inline mapping from the V2 snapshot.
  - `startExecutionInBackground`: removed the `syncLegacyStateView` call and the unused snapshot load.
  - Command handler (`/forge <prd>`): replaced `computeEffectiveStatus(snapshot, state)` with `snapshot?.status ?? "idle"`.
  - `session_start`: removed `syncLegacyStateView` and `attemptSessionRestore` calls; V1 state is no longer restored as authoritative.
- **`src/v1-runtime-quarantine.ts`** → moved to **`src/migration/v1-runtime-quarantine.ts`**.
- **`src/v1-status-helpers.quarantine.ts`** → moved to **`src/migration/v1-status-helpers.quarantine.ts`**.

### Tests run
```bash
node --test --experimental-strip-types tests/integration/no-v1-authority.structural.test.ts
```
**Result:** 7/7 passed.  
No new TypeScript errors were introduced in `index.ts` (`npx tsc --noEmit` shows no `index.ts` errors).

### Remaining caveats
- The broader project has pre-existing `tsc` errors in other modules (unrelated to this task). The structural test and `index.ts` compile cleanly.
- A small amount of legacy V1 `state` variable surface remains inside `index.ts` for deprecated execution paths, but it is no longer synced from or treated as authoritative against the V2 snapshot.